import 'package:flutter/material.dart';

class ControlAnemiaScreen extends StatelessWidget {
  const ControlAnemiaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}